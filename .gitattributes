# 统一换行符，避免 Windows / macOS 协作时整文件 diff
* text=auto eol=lf

# 二进制资源不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary

# 脚本按平台约定
*.sh text eol=lf
*.bat text eol=crlf
*.ps1 text eol=crlf
